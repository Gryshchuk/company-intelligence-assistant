# Copy to .env and fill in real keys. Never commit the populated .env.

# Required — used by the chat model, embeddings, and disambiguation agent.
OPENAI_API_KEY=

# Required for the research agent's web_search tool.
TAVILY_API_KEY=
